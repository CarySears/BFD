### Bronxville Family Dental (static recreation)

This repo contains a **static snapshot recreation** of `bronxvillefamilydental.com` pages for local viewing.

### Run locally

From the repo root:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000/`.

